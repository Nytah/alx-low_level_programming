#include "main.h"
#include <stdio.h>
/**
 * _isdigit - check if a variable is uppercase.
 * @c: passed in alphabet
 * Return: Either 1 or 0.
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}

